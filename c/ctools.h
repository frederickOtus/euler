#include <stdbool.h>

int ind_of_next(char *text, char targ);
void str_partial_cpy(char *dest, char *src, int numchars);

long strToLong(char *string, long);

int *genFibb(int);

long *gen_primes(long);
int magnitude(long);
int nth_dig(long,int);
int is_palindrome(long);
int num_in(int,long);
unsigned long zero_nth_bit(unsigned long, int);
int nth_bit(unsigned long, int);
