#include <stdio.h>
#include <stdlib.h>
#include "tools.h"

main(int argc, char *argv[]){
    /*
    if(argc > 1){
        int tst1 = atoi(argv[1]);
        //int tst2 = atoi(argv[2]);
        unsigned long input = (unsigned long)tst1;
        long *out = gen_primes(input);
        int t = 0;
        printf("In: %d, found: %lu\n", tst1, *(out-1));
        for(t;t<*(out-1);t++){
            printf("%d-th prime:%lu\n", t, *(out+t));
        }
        return 0;
    }else{
        return -1;
    }
    unsigned long l = 4;
    */
    unsigned int a = -1;
    unsigned long b = -1;

    printf("%u, %lu\n", a, b);
}
